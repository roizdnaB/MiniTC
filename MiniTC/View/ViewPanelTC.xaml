<UserControl x:Class="MiniTC.View.ViewPanelTC"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resx="clr-namespace:MiniTC.Properties"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="400" x:Name="Panel">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="13*"/>
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="10">
            <Label VerticalAlignment="Center" Margin="0,0,5,0" 
                   Content="{Binding Source={x:Static resx:Resources.PathLabel}}"/>
            <Label Grid.Column="1" BorderThickness="1"  BorderBrush="Black" Width="300" 
                   Height="27" Background="White"
                   Content="{Binding ElementName=Panel, Path=CurrentPath}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal" Margin="10">
            <Label  VerticalAlignment="Center"  Margin="0,0,5,0" 
                    Content="{Binding Source={x:Static resx:Resources.DriveLabel}}" />
            <ComboBox Width="60" Height="25" HorizontalAlignment="Right"
                      ItemsSource ="{Binding ElementName=Panel, Path=Drivers}" 
                      SelectedItem="{Binding ElementName=Panel, Path=CurrentDrive}"/>
        </StackPanel>

        <ListBox Grid.Row="2" Margin="10" x:Name="lbx"
                 ItemsSource ="{Binding ElementName=Panel, Path=CurrentFolderContent}"
                 SelectedItem="{Binding ElementName=Panel, Path=CurrentFolder}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <ContentControl>
                            <TextBlock Text="{Binding Mode=OneWay}"/>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseDoubleClick">
                                    <i:InvokeCommandAction Command="{Binding ElementName=Panel, Path=DoubleClickCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </ContentControl>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>